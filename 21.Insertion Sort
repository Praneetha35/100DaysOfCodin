def insertionsort(ar,n):
    for i in range(0,n):
        j=i 
        while(j>=1):
            if(ar[j]<ar[j-1]):
                temp=ar[j-1]
                ar[j-1]=ar[j]
                ar[j]=temp
            
            j-=1
    return ar


ar=list(map(int,input().split()))
n=len(ar)
print(insertionsort(ar,n))
