def bubblesort(arr):
    for i in range(0,len(arr)):
        print(arr[i])
        for j in range(0,len(arr)-1-i):
            if(arr[j]<arr[j+1]):
                temp=arr[j+1]
                arr[j+1]=arr[j]
                arr[j]=temp
        print(arr)

bubblesort([1,3,2,5])
